'use client';
import { Suspense } from 'react';
import { SocialMediaIntegrations } from '@/app/pages/SocialMediaIntegrations';
export default function Page() {
  return <Suspense><SocialMediaIntegrations /></Suspense>;
}
